#!/usr/bin/env bash

{{ if eq .chezmoi.os "darwin" }}
# Install iTerm integration if it isn't already installed
if [[ ! -e "$HOME/.iterm2_shell_integration.fish" ]]; then
  curl -L https://iterm2.com/shell_integration/install_shell_integration_and_utilities.sh | SHELL=fish bash
fi
{{ end }}
