# @canac's personal dotfiles

Uses Chezmoi to manage dotfiles. Install on a new machine with the following command:

```sh
/bin/bash -c "$(curl -fsLS https://raw.githubusercontent.com/canac/dotfiles/main/install.sh)"
```
