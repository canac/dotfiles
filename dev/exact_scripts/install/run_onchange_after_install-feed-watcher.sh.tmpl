#!/usr/bin/env bash

# hash: {{ include "dev/exact_scripts/feed-watcher.ts" | sha256sum }}
# hash: {{ include "dev/exact_scripts/mailbox.ts" | sha256sum }}

# Install feed-watcher whenever it changes
deno install -f \
  --allow-read="$HOME/.local/share/canac/" \
  --allow-write="$HOME/.local/share/canac/" \
  --allow-env=HOME \
  --allow-net \
  --allow-run=mailbox \
  "$HOME/dev/scripts/feed-watcher.ts"
