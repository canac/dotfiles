#!/usr/bin/env bash

input=$(cat)
if [[ -z "$input" ]]; then
  printf "VITE_JWT_SECRET=$(openssl rand -hex 32)\nVITE_AUTH_PASSWORD=$(bw generate --passphrase | cut -d'-' -f1)\n"
else
  printf '%s' "$input"
fi
